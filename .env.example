# No credentials are required. Set these in your shell or environment manager;
# this project does not auto-load .env files.
HF_HOME=
TORCH_HOME=

# Keep strict determinism unless CUDA reports a genuinely unsupported operation.
LEXICAL_AMBIGUITY_WARN_ONLY_DETERMINISM=0
